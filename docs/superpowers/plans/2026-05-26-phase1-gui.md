# Phase 1 GUI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade `python run_gui.py` into a traceable Phase 1 Tkinter workbench.

**Architecture:** Add a testable `voltage_margin/gui/phase1_backend.py` for pipeline execution, output loading, filtering, trace formatting, and source-line lookup. Rework `voltage_margin/gui/app.py` to consume that backend and present a polished engineering workbench with filters, tabs, and margin trace details.

**Tech Stack:** Python, Tkinter/ttk, pandas, matplotlib, pytest.

---

### Task 1: Backend API

**Files:**
- Create: `voltage_margin/gui/phase1_backend.py`
- Test: `tests/test_phase1_gui_backend.py`

- [x] Write tests for `run_phase1_pipeline`, `load_output_package`, `filter_margins`, `format_margin_trace_detail`, and `read_source_line`.
- [x] Run tests and verify they fail because the backend does not exist.
- [x] Implement backend functions by reusing existing core modules and output schema.
- [x] Run backend tests and verify they pass.

### Task 2: Workbench GUI

**Files:**
- Modify: `voltage_margin/gui/app.py`

- [x] Replace legacy-only layout with Phase 1 controls, filter sidebar, notebook tabs, plot panel, and trace detail panel.
- [x] Wire Run to `run_phase1_pipeline`.
- [x] Load tables with `load_output_package`.
- [x] Populate pass-rate, margin, sensitivity, warning, and trace tabs.
- [x] Wire filter controls to `filter_margins`.
- [x] Wire selected margin row to `format_margin_trace_detail`.
- [x] Add buttons for copy path:line, open source file, show raw row, open output folder, export current table, and save plot.

### Task 3: Verification

**Files:**
- Modify only if verification exposes issues.

- [x] Run `.venv/bin/python -m pytest -q`.
- [x] Run `.venv/bin/python -m py_compile run_gui.py voltage_margin/gui/app.py voltage_margin/gui/phase1_backend.py`.
- [ ] Manually launch `python run_gui.py` where a display is available.
